{
    "name": "ts-mixin-class-declaration-consumer-fixture-suite",
    "private": true,
    "type": "module",
    "packageManager": "pnpm@11.7.0",
    "scripts": {
        "build": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\" && tsc -p tsconfig.json",
        "test": "siesta dist",
        "prepare": "ts-patch install"
    },
    "dependencies": {
        "ts-mixin-class-fixture-suite": "workspace:*",
        "ts-mixin-class": "workspace:*"
    },
    "devDependencies": {
        "@bryntum/siesta": "catalog:",
        "ts-patch": "catalog:",
        "typescript": "catalog:"
    }
}
