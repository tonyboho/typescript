{
    "name": "ts-lazy-property-basic-project",
    "version": "0.1.0",
    "type": "module",
    "private": true,
    "scripts": {
        "typecheck": "tsc -p tsconfig.json --noEmit",
        "build": "tsc -p tsconfig.json"
    },
    "dependencies": {
        "ts-lazy-property": "workspace:*"
    },
    "devDependencies": {
        "ts-patch": "4.0.1",
        "typescript": "6.0.3"
    }
}
